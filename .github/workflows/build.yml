name: CI

on:
  push:
    tags:
      - '*'
  pull_request:
    branches:
      - '*'

jobs:
  build:
    name: ${{matrix.name}}
    runs-on: ${{matrix.os}}
    strategy:
      matrix:
        os: [windows-2019, ubuntu-18.04]
        include:
          - os: windows-2019
            name: windows
          - os: ubuntu-18.04
            name: linux
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          submodules: recursive
      - name: Configure CMake
        shell: bash
        run: |
          cd $GITHUB_WORKSPACE
          mkdir build && cd build
          cmake .. -DCMAKE_BUILD_TYPE=Release
      - name: Build
        working-directory: ./build
        shell: bash
        run: cmake --build . --config Release
      - name: Prepare to upload artifact
        shell: bash
        run: |
          cd $GITHUB_WORKSPACE
          mkdir artifact
          if [ "$RUNNER_OS" == "Windows" ]; then
            cp ./build/bin/Release/cycle-custom-layouts.dll artifact/
          else
            cp ./build/bin/Release/cycle-custom-layouts.so artifact/
          fi
          cp license.txt cycle-custom-layouts-plugin-settings.json artifact/
      - name: Upload artifact
        uses: actions/upload-artifact@v2.2.2
        with:
          name: ${{matrix.name}}
          path: artifact
  release:
    if: github.event_name == 'push'
    needs: build
    runs-on: ubuntu-18.04
    steps:
      - uses: actions/checkout@v2
      - uses: actions/download-artifact@v2
      - id: tag
        uses: dawidd6/action-get-tag@v1.0.0
      - uses: rlespinasse/git-commit-data-action@1.1.0
      - name: Create .7z and .zip files
        run: |
          cd windows
          7z a vvctre-plugin-cycle-custom-layouts-${{steps.tag.outputs.tag}}-windows.7z cycle-custom-layouts.dll license.txt cycle-custom-layouts-plugin-settings.json
          7z a vvctre-plugin-cycle-custom-layouts-${{steps.tag.outputs.tag}}-windows.zip cycle-custom-layouts.dll license.txt cycle-custom-layouts-plugin-settings.json

          cd ../linux
          7z a vvctre-plugin-cycle-custom-layouts-${{steps.tag.outputs.tag}}-linux.7z cycle-custom-layouts.so license.txt cycle-custom-layouts-plugin-settings.json
          7z a vvctre-plugin-cycle-custom-layouts-${{steps.tag.outputs.tag}}-linux.zip cycle-custom-layouts.so license.txt cycle-custom-layouts-plugin-settings.json
      - id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
        with:
          tag_name: ${{github.ref}}
          release_name: ${{steps.tag.outputs.tag}}
          body: ${{env.GIT_COMMIT_MESSAGE_BODY}}
          draft: false
          prerelease: false
      - name: Upload Windows 7z
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
        with:
          upload_url: ${{steps.create_release.outputs.upload_url}}
          asset_path: ./windows/vvctre-plugin-cycle-custom-layouts-${{steps.tag.outputs.tag}}-windows.7z
          asset_name: vvctre-plugin-cycle-custom-layouts-${{steps.tag.outputs.tag}}-windows.7z
          asset_content_type: application/x-7z-compressed
      - name: Upload Windows zip
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
        with:
          upload_url: ${{steps.create_release.outputs.upload_url}}
          asset_path: ./windows/vvctre-plugin-cycle-custom-layouts-${{steps.tag.outputs.tag}}-windows.zip
          asset_name: vvctre-plugin-cycle-custom-layouts-${{steps.tag.outputs.tag}}-windows.zip
          asset_content_type: application/zip
      - name: Upload Linux 7z
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
        with:
          upload_url: ${{steps.create_release.outputs.upload_url}}
          asset_path: ./linux/vvctre-plugin-cycle-custom-layouts-${{steps.tag.outputs.tag}}-linux.7z
          asset_name: vvctre-plugin-cycle-custom-layouts-${{steps.tag.outputs.tag}}-linux.7z
          asset_content_type: application/x-7z-compressed
      - name: Upload Linux zip
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
        with:
          upload_url: ${{steps.create_release.outputs.upload_url}}
          asset_path: ./linux/vvctre-plugin-cycle-custom-layouts-${{steps.tag.outputs.tag}}-linux.zip
          asset_name: vvctre-plugin-cycle-custom-layouts-${{steps.tag.outputs.tag}}-linux.zip
          asset_content_type: application/zip
